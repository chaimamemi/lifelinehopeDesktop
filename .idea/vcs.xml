<project version="4">
  <component name="VcsDirectoryMappings">
    <!-- Keep only one mapping for Git -->
    <mapping directory="$PROJECT_DIR$" vcs="Git" /> <!-- Use the project directory for Git -->
  </component>
</project>
